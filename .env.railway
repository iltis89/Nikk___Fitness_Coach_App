# Railway Environment Variables für Quick-Deploy (Demo-Modus)

# Basis-Konfiguration
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_DEMO_MODE=true

# Build-Optimierungen
NODE_OPTIONS=--max-old-space-size=4096
NEXT_TELEMETRY_DISABLED=1
NEXT_BUILD_TIMEOUT=60000
NEXT_COMPILE_TIMEOUT=60000
NEXT_BUILD_CACHE=true

# JWT Secret für Demo (in Production durch sicheren Wert ersetzen!)
JWT_SECRET=demo-secret-only-for-testing-replace-in-production

# API URL (nicht benötigt im Demo-Modus)
NEXT_PUBLIC_API_URL=http://localhost:3001